import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const InstructorReviewsPage = () => (
  <PageWrapper variant="instructor">
    <ComingSoon title="Reviews" description="Distribution charts and reply workflows are on the roadmap." />
  </PageWrapper>
);
