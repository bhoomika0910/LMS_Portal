import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const InstructorAnnouncementsPage = () => (
  <PageWrapper variant="instructor">
    <ComingSoon title="Announcements" description="Course-wide broadcasts and scheduling will arrive with the communication tools." />
  </PageWrapper>
);
