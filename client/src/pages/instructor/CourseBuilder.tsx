import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const InstructorCourseBuilderPage = () => (
  <PageWrapper variant="instructor">
    <ComingSoon title="Course Builder" description="Curriculum trees, media uploads, and pricing tabs are queued for the builder milestone." />
  </PageWrapper>
);
