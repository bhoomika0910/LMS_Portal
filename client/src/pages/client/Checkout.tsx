import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const CheckoutPage = () => (
  <PageWrapper>
    <ComingSoon title="Checkout" description="Embedded Stripe elements and frictionless funnel flows are slated for the payments milestone." />
  </PageWrapper>
);
