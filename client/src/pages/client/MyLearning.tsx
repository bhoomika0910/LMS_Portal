import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const MyLearningPage = () => (
  <PageWrapper>
    <ComingSoon title="Student Dashboard" description="Progress analytics, certificates, and watch history dashboards arrive in Step 14." />
  </PageWrapper>
);
