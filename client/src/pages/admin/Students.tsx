import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const AdminStudentsPage = () => (
  <PageWrapper variant="admin">
    <ComingSoon title="Student Management" description="Advanced tables with filters, bulk actions, and exports will land alongside the admin portal build." />
  </PageWrapper>
);
