import { PageWrapper } from '../../components/layout/PageWrapper';
import { ComingSoon } from '../common/ComingSoon';

export const AdminInstructorsPage = () => (
  <PageWrapper variant="admin">
    <ComingSoon title="Instructor Management" description="Revenue share controls and invite modals are queued for the admin experience phase." />
  </PageWrapper>
);
